<% /* params: status ('fresh'|'frozen') */ %>
<% if (status === 'fresh') { %>
  <span class="pill pill-fresh"><span class="status-dot bg-clay"></span> Fresh</span>
<% } else { %>
  <span class="pill pill-frozen"><span class="status-dot bg-frost"></span> Frozen</span>
<% } %>
